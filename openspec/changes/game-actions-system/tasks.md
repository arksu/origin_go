# Tasks

## 1. Protocol and data foundations

- [ ] 1.1 Add to `api/proto/packets.proto`: `C2S_ActivateAction{action_id}`, `S2C_ActiveCursorChanged{cursor}` (empty string = reset), `S2C_ActionList{repeated ActionDefInfo}` with `ActionDefInfo{id, label, cursor, target_kind, required_skills}`; remove `C2S_LiftPutDown`. Verify `make proto` and `cd web_new && npm run proto` regenerate cleanly and `go build ./...` passes
- [ ] 1.2 Create `internal/actiondefs` (types.go, loader.go, registry.go) mirroring itemdefs/builddefs with fields id, label, cursor, target_kind (object|tile|none), required_skills; duplicate id or malformed file fails startup with a `LoadError` naming the file. Verify with loader unit tests covering a valid load, duplicate-id failure, and malformed-JSON failure
- [ ] 1.3 Create `data/actions/lift.json` (target object, no skills) and `data/actions/lift_down.json` (target tile, no skills), and add a startup consistency check that every registered handler has a def and every def has a handler. Verify with an integration test loading `data/actions` and asserting both defs resolve

## 2. Server action service

- [ ] 2.1 Add `ArmedAction{ActionID}` ECS component and wire `ActionService` (`internal/game/action_service.go`) with the def registry, an `ArmedActionHandler` registry (per design D2), a mini-alert sender, and a cursor sender interface. Verify with a unit test that arming a known action sets the component
- [ ] 2.2 Implement activation: `C2S_ActivateAction` handling that validates existence, `containsAllStrings(profile.Skills, def.RequiredSkills)`, and the per-handler state condition; success sets `ArmedAction` and sends `S2C_ActiveCursorChanged{cursor}`; failure sends a mini-alert and changes nothing; re-activating the armed action disarms and sends an empty-cursor reset; arming clears any pending context action. Verify with unit tests for each branch (arm, locked, not-carrying, toggle-off)
- [ ] 2.3 Implement the lift handler: armed lift + object click sets `PendingContextAction{target, action_id}` and lets ordinary link-intent movement proceed; on `LinkCreated` validate the target and call the existing lift entry (rename `StartLiftFromContextAction` → `StartLift`); validation failure alerts and keeps armed. Verify with a test that armed lift on a liftable object ends in carry state and on a non-liftable object alerts without movement
- [ ] 2.4 Implement the lift_down handler: armed tile click calls `LiftService.HandleLiftPutDown` with the clicked position; success clears carry, disarms, resets cursor; rejection alerts and stays armed. Verify with tests for valid placement, rejected placement (still armed), and lift_down activation while not carrying
- [ ] 2.5 Add the MapClick interception branch in `internal/ecs/systems/network_command.go`: after the admin-pending check, call `ActionService.HandleArmedClick` which consumes the click and dispatches by def target kind (object → handler object path via pending, tile → handler tile path immediately); armed object-action + empty ground falls through to ordinary movement staying armed. Verify the existing map-click and admin test suites pass unchanged for the no-armed case, plus new tests for the armed precedence scenarios
- [ ] 2.6 Remove lift from context menus: `lift_behavior.go` stops implementing `ContextActionProvider`; move its target validation into the lift handler; update or remove lift context-menu tests. Verify `S2C_ContextMenu` for a liftable object contains no lift entry in a test
- [ ] 2.7 Remove the `C2S_LiftPutDown` server path (command_queue job, network_command handler, game.go wiring) and delete `sendLiftPutDown` usage. Verify `go build ./...` and `go test ./internal/...` pass with no references remaining
- [ ] 2.8 Add `JobSendActionListSnapshot` queued in the enter-world sequence next to the craft list job, sending `S2C_ActionList`, and send `S2C_ActiveCursorChanged{""}` in the snapshot sequence. Verify with an enter-world test asserting both packets are sent
- [ ] 2.9 Wire state-loss disarm: `LiftService.clearCarryStateForPlayer` calls an injected disarm hook that clears `ArmedAction` and resets the cursor when the armed action is `lift_down`. Verify with a test: armed lift_down + forced drop → armed cleared and cursor reset sent

## 3. Client

- [ ] 3.1 Add network plumbing: `sendActivateAction` in `network/index.ts`, `S2C_ActiveCursorChanged` and `S2C_ActionList` handling in `MessageDispatcher.ts`/`handlers.ts`, and `gameStore.activeCursor` + `gameStore.actionList` state. Verify type-check passes and a manual socket log shows the action list at login
- [ ] 3.2 Create `game/hud/cursorCatalog.ts` (static id → {path, hotspot} map over `/assets/cursor/*.png`, verify frame dimensions during implementation) and a `CursorManager` that watches `gameStore.activeCursor`, applying `cursor: url(...) x y, help` on the game-window container and restoring the default on empty id. Verify manually: arm lift → lift cursor; send unknown id → help cursor; empty → default
- [ ] 3.3 Widen the hotbar: `actionCatalog.ts` keeps the closed window-opener union and adds gameplay id support (`HotbarAssignment = ActionId | GameplayActionId | null`), `useHotbarAssignments` parsing treats ids not in the catalog or server list as empty slots. Verify with unit tests for parse/normalize of mixed and stale assignments
- [ ] 3.4 Create `ActionsMenu.vue` listing `gameStore.actionList` (label + cursor PNG icon), marking `lift_down` unavailable while `!liftCarryActive` and skipping local dispatch for unavailable actions; wire the stubbed `actions` case in `GameView.vue` to open it. Verify type-check/lint pass and the menu renders the two actions
- [ ] 3.5 Route activation: menu selection and hotbar slot activation for gameplay ids call `sendActivateAction`; remove `liftPutDownModeActive`, its toggle, and the direct `sendLiftPutDown` call; drive `LiftGhostController` arming from "lift_down is armed" store state. Verify manually: arm lift → click rock → carrying; arm lift_down → ghost → click ground → placed; Esc-free toggle via re-activation resets cursor

## 4. Full verification

- [ ] 4.1 Run the full server suite (`go test ./...`) and client checks (`npm run type-check`, `npm run lint`) and fix fallout. Verify all green
- [ ] 4.2 End-to-end pass over the spec scenarios: context menu on liftable object has no lift entry; arm from menu and from hotbar; locked-action rejection (test def with a skill); unknown cursor id → help; server reset → default cursor; armed lift on non-liftable object alerts and does not move; admin `/destroy` outranks an armed action. Verify each observable behavior manually or via tests added above
