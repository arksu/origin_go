# Tasks

## 1. Definitions and protocol

- [x] 1.1 Add C2S_ActivateAction, C2S_CancelAction, S2C_ActionList (definition fields, availability, reason), and S2C_ActionStateChanged (action ID, phase, cursor) to api/proto/packets.proto. Keep C2S_LiftPutDown in place for now; its server references survive until the lift migration removes them (see 3.5). Regenerate Go and web_new bindings; verify `make proto`, `cd web_new && npm run proto`, and `go build ./...`.
- [x] 1.2 Implement internal/actiondefs types, loader, and registry with separate presentation, target, requirements, and execution sections. Validate safe menu icon asset paths, target kinds, optional cursor (forbidden for none), isRepeatable rejected on none-target definitions, non-negative independent ticks/stamina, skill IDs, equipment slot names, and exactly one item key/tag per equipment entry. Duplicate/malformed/missing-handler definitions fail startup with a file-identifying error. Cover valid, invalid, duplicate, and handler-mismatch cases in loader tests.
- [x] 1.3 Add data/actions/lift.json and lift_down.json with menu icon asset paths, object/tile targets, their cursors, isRepeatable false, and no action-specific ticks/stamina. Verify both load and resolve to registered handlers. Keep tile and none test actions in test fixtures only.

## 2. Generic server action lifecycle

- [x] 2.1 Add ActionService and action-owned ECS state for idle/selecting/approaching/executing, with handler outcomes that distinguish rejected target, deferred work, success, and failure. Send authoritative S2C_ActionStateChanged for each transition. Test activation of a targeted action, completion, rejection, and state reset.
- [x] 2.2 Implement requirements evaluation: all required skills, all equipment entries with any listed slot per entry, handler state condition, and enough stamina for one execution. Use CharacterProfile, InventoryRefIndex/InventoryContainer, and itemdefs. Recheck on activation, target acceptance, relevant state changes, and completion. Test key/tag, multiple entries, unavailable state, and lost requirements.
- [x] 2.3 Add enter-world ActionList and current action-state snapshots. Include server-computed availability and reason codes; refresh the list after skill, equipment, carry, or supported requirement changes. Verify login and each state-change update without exposing skill membership through client guesses.
- [x] 2.4 Route C2S_ActivateAction and C2S_CancelAction. Re-activation while selecting toggles off; a different activation cancels old selection/approach/cycle without stamina charge, stops action-initiated movement, and attempts the new action. If the new one fails validation, report its reason and remain idle. Escape cancellation must also clear action-owned pending work. Test switching during approach and a running cycle, cancellation, and unavailable replacement.
- [x] 2.5 Add one MapClick interception branch after administrator pending commands and before ordinary pickup/link/movement. Dispatch object and tile targets by definition kind only; object clicks on live collider or no-collider objects are consumed, empty/stale object targets fall through to ordinary movement, and tile actions consume every click. Test admin precedence, invalid-object rejection without movement, no duplicate pickup, and unchanged no-action routing.
- [x] 2.6 Implement untimed execution and timed execution using ActiveCyclicAction, including self-target for none. Route new cycles from CyclicActionSystem to ActionService before context-behavior lookup. Keep ticks and stamina independent; precheck affordability and handler effect, avoid partial effects on failure, charge stamina exactly once after successful handler completion, and emit existing cycle progress/finish for timed actions. Synthetic handlers must test none/tile/object, zero cost, instant cost, timed cost, failure, and cancel-without-cost.
- [x] 2.7 Implement completion policy: isRepeatable true on a target action returns to selecting after success if requirements still hold; false ends and resets; none always runs once. Target-level failure allows retry while requirements hold. Test repeatable and non-repeatable action states and cursor packets.
- [x] 2.8 Cancel active Actions on requirement/state loss and clean them on death, leave-world, and reconnect. Stop only action-owned movement and prevent stale target/cycle callbacks from completing a canceled action. Test loss while selecting and while executing, plus a late completion callback after cancellation.

## 3. Migrate lift and put-down completely

- [x] 3.1 Remove lift from context-action enumeration and remove implicit no-collider lift from the Interact path. Keep lift target validation in the action handler/LiftService. Verify neither context menu nor ordinary Interact offers or starts lift.
- [x] 3.2 For collider objects, give armed lift an action-owned pending target and reuse move-to-link movement. On LinkCreated, ActionService revalidates and calls a new LiftService.StartLift directly, without ContextActionService.ExecuteAction or PendingContextAction; the context-coupled StartLiftFromContextAction is removed. Test a liftable target, a non-liftable target before movement, target loss during approach, and cancellation while approaching.
- [x] 3.3 Move the no-collider phantom-collider/PendingLiftTransition path behind armed lift and report its final success, rejection, and timeout to ActionService. Test that a no-collider object lifts only when armed and that failure leaves lift selecting while requirements hold.
- [x] 3.4 Refactor LiftService put-down entry to take the action-selected coordinates instead of C2S_LiftPutDown, retain its deferred placement validation, and report final success/failure/timeout. Immediate and final rejection leave lift_down selecting while carry remains; success ends it. Wire forced carry loss to cancellation, avoiding duplicate terminal action-state packets. Test valid placement, rejected placement, timeout, and forced drop.
- [x] 3.5 Remove obsolete server command queue, network handler, and game wiring for C2S_LiftPutDown, then delete the message from api/proto/packets.proto and reserve its retired ClientMessage field number/name. Regenerate Go and web_new bindings (`make proto`, `cd web_new && npm run proto`). Verify no maintained server/client sender references remain and `go build ./...` passes.

## 4. Client menu, cursor, Escape, and hotbar

- [x] 4.1 Add network senders/dispatch/handlers for activate, cancel, ActionList, and ActionStateChanged. Store the server action ID, phase, cursor, definitions, availability, and reasons in gameStore. Verify generated types and that login receives both list and state.
- [x] 4.2 Make clicking Actions in the left HUD rail toggle a compact non-modal panel beside the button. Render one non-wrapping horizontal row of server-ordered icons, with horizontal scrolling when needed; show accessible labels, unavailable reasons on hover/focus, and a visible/accessible selected state for the active action. Unavailable icons must use aria-disabled and remain keyboard-focusable without activating. Toggle closed on repeated Actions click, close on outside click, and do not close on inside click. Selecting an available icon activates and closes the panel. Verify ordering, active styling, outside click, and lift_down availability after carry changes.
- [x] 4.3 Add cursorCatalog and CursorManager for optional target cursors. Known IDs use cursor PNG/hotspots; unknown IDs use CSS help; empty IDs restore default. Drive LiftGhostController from authoritative lift_down/selecting state, not cursor equality. Remove liftPutDownModeActive, direct sendLiftPutDown, and the separate lift-down HUD button. Verify arm, server reset, unknown cursor, and ghost behavior.
- [x] 4.4 Give active-action cancellation first priority in the GameView Escape handler. Send C2S_CancelAction and wait for authoritative idle state before resetting the cursor; leave an open window for a later Escape. Verify Escape while selecting and while an action cycle/approach is active.
- [x] 4.5 Support dragging action icons from the panel to hotbar slots and activating gameplay IDs while retaining window openers. Keep stored gameplay IDs until ActionList arrives; then render server-absent IDs as empty/inert without deleting valid assignments during loading. Test mouse/touch drag, mixed entries, login timing, stale IDs, and menu/hotbar equivalence.

## 5. Verification

- [x] 5.1 Run go test ./..., go build ./..., client type-check/lint, and relevant client tests. Fix fallout from protocol and lift entry-point removal.
- [x] 5.2 Exercise end-to-end scenarios: menu availability and reason refresh; arm lift from menu and hotbar; collider and no-collider lift; put-down ghost, valid and rejected placement; Escape before an open window closes; switch during approach/cycle; forced carry loss; unknown cursor fallback; admin /destroy precedence; and ordinary map clicks with no active action. Record test or manual evidence for each.
- [x] 5.3 Remove transient executing packets for untimed actions, cancel an approach before routing a new ordinary map click, and specify that any selecting action suppresses item-in-hand drop. Cover packet and click ordering with regression tests.
