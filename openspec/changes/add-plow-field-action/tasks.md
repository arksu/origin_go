# Tasks

## 1. Action definition and scope

- [x] 1.1 Add optional `target.approach: "tile_center"` to action definitions; reject it on non-tile targets and preserve the default direct behavior. Verify loader tests for valid tile, invalid non-tile, and unchanged lift_down definitions.
- [x] 1.2 Add `data/actions/plow_tile.json` with label Plow tile, `/assets/cursor/dig.png`, tile-center approach, `dig` cursor, 20 ticks, absolute `stamina: 250`, empty requirements, and repeat mode. Ensure `dig` is registered in the client cursor catalog using its existing asset. Verify the loader accepts the definition, the action catalog contains its existing absolute cost fields without changing the action-catalog protobuf format, and the cursor resolves to the dig asset instead of the fallback.

## 2. Confirmed arrival and action lifecycle

- [x] 2.1 Record point-target arrival in movement processing and publish a one-shot stop/arrival event after collision resolution and transform application, carrying the actual player position and intended point. Verify that an unobstructed arrival fires once and a collision-shortened move never reports a false successful arrival.
- [x] 2.2 Normalize an opt-in tile click to signed tile indices and its center, then approach that center even when the player stands inside the tile away from its center. Start the cycle directly only within movement arrival tolerance. Verify click positions within one tile select the same center, negative coordinates resolve correctly, and lift_down remains unchanged.
- [x] 2.3 Size the opt-in approach timeout for distance and conservative movement speed; start the cycle only after confirmed arrival at the remembered center. Verify a reachable distant tile does not hit the old fixed 15-second limit and a blocked center eventually ends the attempt without effect or action stamina charge.
- [x] 2.4 Let a click during an opt-in tile approach retarget to another valid tile center; reject an invalid retarget with an alert while keeping the original approach. Verify both cases and that neither click falls through to ordinary map movement.
- [x] 2.5 Revalidate the target and the player's actual center position immediately before a timed opt-in tile action's effect; do not track departures during execution. Verify being outside movement arrival tolerance at completion causes no tile change or action stamina charge, while leaving and returning within tolerance before completion permits success when all other checks pass.
- [x] 2.6 Keep a repeatable target action Selecting after success, failed approach, invalid target, or temporary stamina shortage until explicit cancellation or a non-stamina requirement loss. Reject low-stamina armed clicks without movement. Verify the cursor remains armed after spending the last affordable 250 units and after an insufficient-stamina click.
- [x] 2.7 Give active object/tile actions priority over dropToWorld in client primary-map-click routing for Selecting, Approaching, and Executing. Resolve activity from authoritative action state and catalog target kind, not from a non-empty visual cursor. Verify that an item in hand remains held, each click sends MapClick without dropToWorld, accepted and rejected approach retargets reach the server, executing clicks are not queued as later plows, and ordinary hand-item dropping still works without an active target action.

## 3. Chunk mutation, saving, and visibility

- [x] 3.1 Make `Chunk.SetTile` the single tile mutation point: bounds-checked single-byte write, per-tile passability/swimmability bit update that clears stale bits instead of only setting them (`populateTileBitsets` currently only ORs bits in), Version increment, and tilesDirty update; skip unchanged IDs; zero both bitsets before repopulating on the load path so restores cannot leave stale bits. Verify the changed tile, unchanged neighbors, bitsets, version, bounds behavior, and that a restore–modify–restore cycle — including a `SetTile` to a less-permissive tile — leaves no stale passable/swimmable bits.
- [x] 3.2 Restore the persisted version on load and store it on save: replace the load path with a restore that installs `Tiles + Version` without incrementing; change `UpsertChunk` to store the passed Version, reject a write whose Version is lower than the stored one, and return the affected-row count; migrate `chunk.version` to `NOT NULL DEFAULT 0` (no existing data, no compatibility) and regenerate sqlc. Adapt every caller, including `cmd/mapgen/main.go`: newly generated chunks pass Version 0, and mapgen treats a result other than one affected row as a failed write. Verify a generated chunk begins at version 0, a loaded chunk continues from its stored version, a plow increments from it, a save stores exactly the snapshot's version, and `go build ./cmd/mapgen` passes with the regenerated query API.
- [x] 3.3 Capture saved tiles, Version, and LastTick under one read lock; treat a zero-row (version-rejected) write as superseded; clear tilesDirty after a successful write only if the saved Version still matches. Make safeSaveAndRemove retain dirty chunks after failed, rejected, or superseded saves and schedule a retry with bounded delay; remove only after final identity/state/interest/dirty checks permit it. Verify an edit during an in-flight save survives loss of AOI interest and is written by a later save, and a DB failure or version rejection retains the chunk for retry without a tight retry loop.
- [x] 3.4 Add shared Chunk.SnapshotTiles that returns a copied Tiles buffer and Version under one read lock. Use it for initial EnableChunkLoadEvents loads, updateEntityAOI loads, and edited-chunk rebroadcasts instead of separate public-field reads. Send the edited snapshot to clients with active interest using their captured stream epochs and event sequences from the shared creation path in 3.6. Verify snapshot bytes cannot change after return, concurrent tile mutation cannot mismatch bytes and Version, another viewer receives the update, and two successive edits contain both tile changes.
- [x] 3.5 Keep a per-coordinate client version guard for the whole world stream, surviving chunk unload and render-cache eviction. Run one shared acceptance decision before gameStore and renderer updates, including the stream/event checks in 3.6: reject a payload with a Version lower than the highest applied for that coordinate; accept an equal Version even without retained geometry and use 3.7 to reuse or build it; on a greater Version, rebuild. Reset all guards and cached terrain on a world-stream reset. Verify out-of-order updates while loaded cannot restore an old tile, a higher reloaded version rebuilds, an equal-version reload after cache eviction is accepted, and a fresh stream accepts its first chunk after a server restart.
- [x] 3.6 Add `uint32 stream_epoch = 2` and `uint64 event_seq = 3` to both S2C_ChunkLoad and S2C_ChunkUnload and regenerate Go/client bindings. Assign event sequences from one per-client world-stream counter, starting at 1, through a shared creation path ordered with authoritative AOI transitions and load snapshot capture before async dispatch; do not reset on unload/eviction or stamp events in async workers. Preserve captured fields through ECS events and dispatchers and recheck epoch immediately before client enqueue. On the client, compare sequences losslessly per coordinate before all store/render/bootstrap effects; reject old epochs and older/duplicate/zero sequences, preserve guards across unloads, and clear them on a new stream. Cancel/guard buffered loads and pending builds so a later unload/reset cannot be undone. End the affected connection if its send queue cannot accept a chunk visibility message. Verify load-before-unload delivered in reverse, unload-before-reload delivered in reverse at equal tile Version, duplicate events, independent chunk coordinates, an old-stream payload after a reset, a buffered load completing after unload, uint64 comparison precision, and send-queue overflow recovery.
- [x] 3.7 Retain ready client Chunk instances on unload and reuse their existing ground geometry on an accepted equal-version reload when the built Version and neighbor inputs match, without calling buildTiles. Keep ChunkManager as the owner of the instance and graphics; use associated cache metadata instead of a separate GPU/geometry extraction cache. Track neighbor presence and tile Version for border/corner dependencies; refresh changed inputs, preserve the own tile Version during border refresh instead of writing 0, and defer work for hidden chunks until reload. Keep decorative terrain's existing clear/regenerate path. Apply existing TTL/LRU settings to hidden ready chunks, exempt active visible chunks, and make eviction cancel work and dispose of both the Chunk and its metadata through one path while preserving acceptance guards. Verify actual instance/geometry reuse, equal-version cache-miss building, higher-version rebuilding, a same-presence neighbor version change refreshing geometry without resetting the own Version, incomplete builds not being reused, TTL/LRU freeing hidden resources exactly once while preserving active chunks, and world reset clearing all resources and pending work.

## 4. Plow tile behavior

- [x] 4.1 Implement and register the `plow_tile` handler with terrain whitelist {coniferous forest, broadleaf forest, thicket, grass}, revalidation on arrival and completion, and no object-occupancy query. Verify each accepted terrain and rejections for already-plowed, disallowed, missing, and changed tiles; verify an object that leaves the center reachable does not cause an occupancy rejection.
- [x] 4.2 On a valid completed cycle, change only the selected tile to TilePlowed and charge exactly 250 stamina through the existing action completion path; on failure change no tile and charge no action stamina. Verify players with different maximum stamina pay the same amount and a failed write or lost affordability has no partial effect.

## 5. Integration and regression

- [x] 5.1 Run the full flow: select Plow tile in Actions, click eligible far and same-tile targets, reach each center, complete a 20-tick cycle, click another tile without reselecting, and observe updates on a second client. Also try an item in hand during target selection and approach retargeting, an obstructed center, disallowed terrain, insufficient stamina, remaining away from the center at completion (failure), leaving and returning before completion (success if otherwise valid), and AOI exit/re-entry after server chunk eviction within the same stream. Verify unchanged tiles and neighbor inputs reuse a retained ready Chunk, an equal-version reload after client TTL/LRU eviction builds again, changed own tiles rebuild, changed neighbor inputs refresh geometry without changing the own Version, and a clean restart restores plowed tiles. Record observed results.
- [x] 5.2 Run focused action, movement, chunk, and client chunk-load tests, then `go test ./...` and `openspec validate "add-plow-field-action" --strict`. Verify all required gates pass and existing lift_down behavior remains intact.
